import { InjectionToken } from '@angular/core';

import { ClientContextRead } from './client-context.read';

export const CLIENT_CONTEXT_READ = new InjectionToken<ClientContextRead>('CLIENT_CONTEXT_READ');
